const errorsMessages: { [k: string]: string } = {
  INVALID_GOOGLE_TOKEN: "Invalid token from Google.",
  USER_NOT_FOUND: "User not found.",
  USER_ALREADY_EXISTS: "User already exists.",
  INVALID_USER_INFORMATION:
    "Invalid user information. Contact our support for more information.",
};

export default errorsMessages;
